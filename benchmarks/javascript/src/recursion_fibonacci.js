// import { strict as assert } from "node:assert";
// import { benchmark, writeBenchmarkResult } from "./utils.js";

// const recursionFibonacci = (n) =>
//   n < 2 ? n : recursionFibonacci(n - 1) + recursionFibonacci(n - 2);

// // Test output
// assert(recursionFibonacci(20) == 6765);

// // Run benchmark
// let times = benchmark(recursionFibonacci, [20], 1000, 100);
// writeBenchmarkResult("recursion_fibonacci", times);
